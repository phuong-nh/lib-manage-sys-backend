spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/library_management_system
    username: library_access
    password: 123456
  jpa:
    hibernate:
      ddl-auto: create
#    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
logging:
  level:
    org:
    hibernate:
      SQL: DEBUG
      type:
      descriptor:
        sql:
        BasicBinder: TRACE
